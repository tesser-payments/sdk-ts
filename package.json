{
  "name": "@tesser-payments/sdk",
  "version": "0.0.1",
  "description": "Signer-only TypeScript SDK for Tesser API: locally stamp signCreateWallet and signStep activities. No HTTP, no OAuth — bring your own client.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "README.md", "LICENSE"],
  "engines": {
    "node": ">=20",
    "bun": ">=1.3"
  },
  "scripts": {
    "build": "bun run build:js && bun run build:types",
    "build:js": "bun build ./src/index.ts --target=node --format=esm --outdir=./dist --external @turnkey/api-key-stamper --external @tesser-payments/types",
    "build:types": "tsc -p tsconfig.build.json",
    "prepack": "bun run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "biome check .",
    "lint:fix": "biome check . --write",
    "typecheck": "tsc --noEmit",
    "release": "changeset publish"
  },
  "peerDependencies": {
    "@tesser-payments/types": "*",
    "@turnkey/api-key-stamper": "*"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9",
    "@changesets/cli": "^2.27",
    "@tesser-payments/types": "*",
    "@turnkey/api-key-stamper": "*",
    "@types/node": "^20",
    "typescript": "^5.4",
    "vitest": "^2"
  }
}
